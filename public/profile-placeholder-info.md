# Profile photo

Drop a square JPG (>= 400×400 recommended) at `public/profile.jpg` before
deploying. The `Hero` component will pick it up automatically. If the file is
missing at runtime, the component falls back to a "BC" monogram — no build
failure, but the real photo is what you want for a hiring-friendly page.
